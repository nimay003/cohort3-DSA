#include<bits/stdc++.h>

using namespace std;

int main(){
    // ios_base::sync_with_stdio(false);
    // cin.tie(NULL);
    // cout.tie(NULL);

    // int arr1[] = {1, 2, 3, 4, 5}; //static
    // int arr2[10]; //dynamic

    int n;
    cout << "Enter number = ";
    cin >> n;
    int arr3[n];

    for(int i = 0; i < n; i++){
        cout << "Enter Array's " << (i + 1) << " Number = ";
        cin >> arr3[i];
    }
}