#include<bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  
  int x = 4;
  string y = (x % 2 == 0) ? "Even" : "Odd";

  cout<<"Number is "<<y<<endl;
}